import React from 'react'
import { View, Text, TextInput, Button, StyleSheet } from 'react-native';

function Saludo() {
    const usuario = {
        nombre: TextInput,
        apellido: TextInput
    }
}